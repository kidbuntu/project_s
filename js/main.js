$(function(){

	// CENTER CONTAINER
	$('.container').layout({
		fit:true,
		border:false
	});

	// TAB CONTAINER
	$('#tab_container').tabs({
		fit:true,
		tabHeight:80,
		// tabWidth:120,
		// selected:5
	});	
});